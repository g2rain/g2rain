# 安全边界

- App 通过 IAM 取得有效凭证，再经 Gateway 访问公开服务接口。
- 内部 URL、内网地址和 OpenAPI 隐藏不能替代服务身份验证和授权。
- 租户标识必须来自可信上下文，不能只相信普通请求参数。
- 请求上下文、查询结果和写入记录的租户必须一致。
- 输入、数据库、日志和响应遵循数据最小化原则。
- 不记录或返回密码、Token、密钥、验证码和非必要个人资料。
- 跨租户、内部接口、身份恢复和敏感字段变更必须增加越权与泄露测试。

Gateway、IAM 等安全基础设施可以有不同 Profile，但领域服务不能把自身安全责任全部转交给它们。

